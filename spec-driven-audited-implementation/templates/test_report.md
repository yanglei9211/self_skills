# 测试报告

> 简练约束：优先写 AC→证据等级→命令→结果。不要重复实现细节；长输出只保留关键行。

## 测试设计模式

- 模式：independent tester / independent session / simulated tester role / main-agent self-check
- 是否独立：
- Tester：
- 与 Implementer 是否分离：是 / 否 / 无法确认
- 与 Auditor 是否分离：是 / 否 / 无法确认

若非独立，写明：`非独立测试设计，仅为 main agent 自检，存在测试偏向实现的风险。`

## 验收标准到测试映射

| 验收标准 | 证据等级 | 测试文件 / 场景 | 命令 | 环境 / 解释器 | 结果 | 是否可证明真实行为 |
|---|---|---|---|---|---|---|
|  | E2E / UNIT / MOCKED / STATIC / UNVERIFIED / ENV_MISMATCH |  |  |  |  | 是 / 否 |

红线：`MOCKED` 不能证明真实外部 API 行为；`STATIC` 不能证明运行时行为；命令在当前环境失败时必须标 `ENV_MISMATCH`。

## 测试策略

- Happy path：
- 边界情况：
- 失败路径：
- 回归风险：
- Mock 使用说明：

## 测试命令与结果

```text
<粘贴命令和关键输出>
```

## Fresh Verification

- 是否由 Tester fresh run：
- 实际可复现命令：
- 与 artifact 中记录命令是否一致：是 / 否
- 如果不一致，原因：
- 当前环境失败但其他环境通过的项目（标 ENV_MISMATCH）：

## 失败与修复记录

- 无 / 

## 测试质量检查

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只测 happy path | 否 / 是 |  |
| 是否覆盖边界情况 | 是 / 否 |  |
| 是否覆盖失败路径 | 是 / 否 |  |
| 是否存在过度 mock | 否 / 是 |  |
| 是否删除、跳过或弱化测试 | 否 / 是 |  |
| 是否测试实现细节而非业务行为 | 否 / 是 |  |

## Point-in-time / Backtesting 检查

| 风险 | 结论 | 证据 |
|---|---|---|
| 未来数据泄漏 | 不适用 / 已检查 / 未覆盖 |  |
| `latest/current/today/now` 隐式当前数据 | 不适用 / 已检查 / 未覆盖 |  |
| 历史日期默认读最新数据 | 不适用 / 已检查 / 未覆盖 |  |
| 全量预加载后过滤导致污染 | 不适用 / 已检查 / 未覆盖 |  |
| 系统日期或文件 mtime 影响结果 | 不适用 / 已检查 / 未覆盖 |  |
| 未来数据存在但不应被读取 | 不适用 / 已检查 / 未覆盖 |  |
| 目标日期快照缺失 | 不适用 / 已检查 / 未覆盖 |  |
| 同一标的不同历史日期可见数据不同 | 不适用 / 已检查 / 未覆盖 |  |
| 未来 spike / crash 不改变 T 日信号 | 不适用 / 已检查 / 未覆盖 |  |
| T 日信号可由 `<= T` 数据独立重算 | 不适用 / 已检查 / 未覆盖 |  |
| 全量 forward 数据无法进入 signal path | 不适用 / 已检查 / 未覆盖 |  |

## 未覆盖风险

| 风险 | 严重度 | 影响的 AC | 是否阻塞接受 | 说明 |
|---|---|---|---|---|
| 无 /  | Critical / Important / Minor |  | 是 / 否 |  |

## 测试结论状态

- 测试结论：ACCEPTED / CONDITIONAL_ACCEPTED / INCOMPLETE / BLOCKED
- 不能标为 ACCEPTED 的原因（如有）：
