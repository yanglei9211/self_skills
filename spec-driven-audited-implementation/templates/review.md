# 代码审查

> 简练约束：只写真实 findings 和高风险检查结论。重复背景、完整文件清单、长篇解释放到原始 artifact 引用中。

## Review 模式

- 模式：independent subagent / independent session / simulated reviewer role / main-agent self-check
- 是否独立：
- Code Reviewer：
- 与 Implementer 是否分离：是 / 否 / 无法确认
- 与 Tester 是否分离：是 / 否 / 无法确认

若非独立，写明：`非独立 review，仅为 main agent 自检，不能视为最终质量保证。`

## Review 输入

- Spec：
- Plan：
- Git diff：
- 测试文件：
- 测试命令与结果：

## Findings

### Critical

| 文件 | 问题 | 影响 | 修复建议 | 必须修复 |
|---|---|---|---|---|
| 无 |  |  |  |  |

### Important

| 文件 | 问题 | 影响 | 修复建议 | 必须修复 |
|---|---|---|---|---|
| 无 |  |  |  |  |

### Minor

| 文件 | 问题 | 影响 | 修复建议 | 必须修复 |
|---|---|---|---|---|
| 无 |  |  |  |  |

## 高风险点检查

| 高风险点 | 结论 | 证据 |
|---|---|---|
| 验收标准覆盖 |  |  |
| Scope creep |  |  |
| 测试不足 |  |  |
| 公共 API / 数据模型变更 |  |  |
| 错误处理与失败路径 |  |  |
| Point-in-time / future leakage（如适用） |  |  |
| 未来数据存在但不应被读取（如适用） |  |  |
| 目标日期快照缺失（如适用） |  |  |
| 同一标的不同历史日期可见数据不同（如适用） |  |  |

## 默认值审查

| 默认值 / 默认命令 | 是否符合 spec | 是否真实运行 | 风险 |
|---|---|---|---|
| 默认 `--from` / 起始日期 | 是 / 否 / 不适用 | 是 / 否 |  |
| 默认 `--to` / 结束日期 | 是 / 否 / 不适用 | 是 / 否 |  |
| 默认解释器 / venv | 是 / 否 / 不适用 | 是 / 否 |  |
| CLI wrapper / subprocess | 是 / 否 / 不适用 | 是 / 否 |  |
| 默认数据库 / 缓存路径 | 是 / 否 / 不适用 | 是 / 否 |  |
| spec 示例命令 | 是 / 否 / 不适用 | 是 / 否 |  |
